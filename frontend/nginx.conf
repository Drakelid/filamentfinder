events {}

http {
  include /etc/nginx/mime.types;
  default_type application/octet-stream;

  server {
    listen 3030;
    server_name _;
    absolute_redirect off;
    port_in_redirect off;

    root /usr/share/nginx/html;
    index index.html;

    location = /health {
      add_header Content-Type text/plain;
      return 200 'ok';
    }

    # Never cache the runtime env config, service worker, or manifest —
    # browsers must always fetch these fresh to detect updates.
    location = /filamentfinder/env-config.js {
      add_header Cache-Control "no-store, no-cache, must-revalidate";
      try_files /env-config.js =404;
    }

    location = /filamentfinder/service-worker.js {
      add_header Cache-Control "no-store, no-cache, must-revalidate";
      try_files /service-worker.js =404;
    }

    location = /filamentfinder/manifest.webmanifest {
      add_header Cache-Control "no-store, no-cache, must-revalidate";
      try_files /manifest.webmanifest =404;
    }

    location /filamentfinder/api/ {
      proxy_pass http://backend:8000/api/;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_set_header X-Forwarded-Host $host;
      proxy_set_header X-Forwarded-Prefix /filamentfinder;
      proxy_connect_timeout 10s;
      proxy_read_timeout 120s;
      proxy_send_timeout 30s;
    }

    location /api/ {
      proxy_pass http://backend:8000;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_set_header X-Forwarded-Host $host;
      proxy_set_header X-Forwarded-Prefix /filamentfinder;
      proxy_connect_timeout 10s;
      proxy_read_timeout 120s;
      proxy_send_timeout 30s;
    }

    location / {
      try_files $uri $uri/ /index.html;
    }
  }
}
